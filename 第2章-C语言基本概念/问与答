问：GCC是什么的简称
答：GCC最初是CNU C Compiler的简称。现在指GNU Compiler Collection，这是因为最新版本的GCC能编译用Ada、
    C、C++和Java等多种语言编写的程序

问：GNU是什么意思
答：GNU([(g)nuː])指的是“GNU's Not UNIX!”，是自由软件基金会的一个项目，GNU项目从头开始重写了许多传
    统UNIX软件，并使公众能够免费获得

问：某些C语言书中，main函数的结尾是exit(0)，而不是return 0;
答：两个语句完全等价，二者都终止程序执行，并向操作系统返回0

问：在一个注释中嵌套另一个注释是否合法
答：传统风格的注释(/**/)不允许嵌套，因为/*遇到*/就会闭合，C99的新注释可以嵌套在传统风格的注释中

问：为什么浮点常量需要以f结尾
答：包含小数却不以f结尾的常量是double型，double型的值比float型的值存储更准确，并且可以存储更大的值；
    如果不加f，编译器会经过存储到float型变量中的数可能超出了该变量的取值范围
